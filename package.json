{
  "name": "lapvisor",
  "version": "0.0.1",
  "description": "CLI for race data analysis — RaceChrono Analytics for the terminal, agent-friendly.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "lapvisor": "./src/cli/index.ts"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/cli/index.ts --compile --outfile dist/lapvisor"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "picocolors": "^1.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
